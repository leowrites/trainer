import type { ProgressiveOverloadConfig } from './types';

export const DEFAULT_PROGRESSION_CONFIG: ProgressiveOverloadConfig = {
  weightIncrement: 5,
  unit: 'kg',
  precision: 1,
};

export const HISTORY_PAGE_SIZE = 20;
