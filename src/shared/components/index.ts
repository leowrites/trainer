/**
 * Shared components
 *
 * Reusable, feature-agnostic UI primitives used across multiple feature slices
 * (e.g., Button, Card, Input, Modal). Components here must have no dependency
 * on any feature slice.
 */
export {};
