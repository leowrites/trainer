/**
 * Shared constants
 *
 * App-wide constant values: feature flags, limits, default settings.
 */

/** Default number of sets for a new exercise in a routine */
export const DEFAULT_SET_COUNT = 3;

/** Maximum allowed weight (kg) for a single logged set */
export const MAX_WEIGHT_KG = 1000;

/** Minimum rest timer duration in seconds */
export const MIN_REST_SECONDS = 10;

/** Maximum rest timer duration in seconds */
export const MAX_REST_SECONDS = 600;

/** Default rest timer duration in seconds */
export const DEFAULT_REST_SECONDS = 90;
